# 配置新的中继接口指南

## 概述

本项目已更新为支持新的 OpenAI 中继接口。以下是配置步骤：

## 已完成的修改

1. **更新了默认 API 基础 URL**

   - `action.yml` 中的 `openai_base_url` 默认值已修改为
     ：`https://api.zetatechs.com/v1`
   - `src/options.ts` 中的 `apiBaseUrl` 默认值已同步更新

2. **兼容性保证**
   - 项目仍然支持原有的配置方式，所有参数名称保持不变
   - 现有的 GitHub Actions 工作流程无需修改

## 配置 API 密钥

### 方法 1：使用 GitHub Secrets（推荐）

1. 在您的 GitHub 仓库中，导航到 **Settings** > **Secrets and variables** >
   **Actions**
2. 点击 **New repository secret**
3. 创建一个名为 `OPENAI_API_KEY` 的密钥
4. 将值设置为：`sk-9jJGTNeyN7T4bBeilg9uNkIttV7ujvSReNJpwl8kh4d85opO`

### 方法 2：在工作流程中直接指定 API URL（可选）

如果您需要在不同的项目中使用不同的 API 端点，可以在
`.github/workflows/ai-pr-reviewer.yml` 中添加：

```yaml
- uses: coderabbitai/ai-pr-reviewer@latest
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  with:
    openai_base_url: 'https://api.zetatechs.com/v1' # 可选，已设为默认值
    debug: false
    review_simple_changes: false
    review_comment_lgtm: false
```

## 新接口特性

- **接口地址**：`https://api.zetatechs.com/v1/chat/completions`
- **请求方式**：POST
- **请求头要求**：
  - `Content-Type: application/json`
  - `Authorization: Bearer {您的API密钥}`

## 验证配置

1. 确保您的 GitHub Actions secrets 中已正确设置 `OPENAI_API_KEY`
2. 提交一个测试 Pull Request
3. 检查 GitHub Actions 日志，确认连接到新的 API 端点
4. 如果需要调试，在工作流程中设置 `debug: true`

## 注意事项

- 新的中继接口应该与 OpenAI API 完全兼容
- 所有现有的配置参数（模型选择、温度、重试次数等）都继续有效
- 如果遇到问题，请检查 GitHub Actions 日志中的详细错误信息

## 支持的模型

确保新的中继接口支持以下默认模型：

- `gpt-3.5-turbo`（轻量级任务，如总结）
- `gpt-4o-mini`（复杂任务，如代码审查）

如果需要使用其他模型，可以在工作流程中通过 `openai_light_model` 和
`openai_heavy_model` 参数指定。
