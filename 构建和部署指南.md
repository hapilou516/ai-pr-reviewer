# AI-PR-Reviewer 构建和部署指南

## 项目概述

AI-PR-Reviewer 是一个基于 OpenAI 的 GitHub Pull Request 代码审查和总结工具。它使
用 OpenAI 的 GPT 模型（如 `gpt-3.5-turbo` 和 `gpt-4`）来自动审查代码变更并提供有
用的反馈。该项目是用 TypeScript 编写的，作为 GitHub Action 运行。

## 环境要求

- Node.js 17+
- npm 或 pnpm 包管理器
- GitHub 账户
- OpenAI API 密钥

## 构建项目

### 1. 克隆仓库

```bash
git clone https://github.com/您的用户名/ai-pr-reviewer.git
cd ai-pr-reviewer
```

### 2. 安装依赖

```bash
npm install
```

或者使用 pnpm：

```bash
pnpm install
```

### 3. 构建项目

```bash
npm run build
```

这个命令会执行以下操作：

- 将 TypeScript 代码编译为 JavaScript
- 复制必要的依赖文件（如 tiktoken_bg.wasm）到 dist 目录

### 4. 打包项目

```bash
npm run package
```

这个命令使用 @vercel/ncc 将所有依赖打包到一个单一的文件中，便于在 GitHub Action
中使用。

### 5. 完整构建流程

您可以使用以下命令一次性执行完整的构建流程：

```bash
npm run all
```

这个命令会执行构建、格式化检查、代码检查、打包和测试。

## 部署到 GitHub

### 1. 创建 GitHub 仓库

1. 登录您的 GitHub 账户
2. 点击右上角的 "+" 图标，选择 "New repository"
3. 填写仓库名称（例如：ai-pr-reviewer）
4. 选择公开或私有仓库
5. 点击 "Create repository"

### 2. 推送代码到 GitHub

```bash
# 初始化 Git 仓库（如果尚未初始化）
git init

# 添加远程仓库
git remote add origin https://github.com/您的用户名/ai-pr-reviewer.git

# 添加所有文件
git add .

# 提交更改
git commit -m "初始提交"

# 推送到 GitHub
git push -u origin main
```

### 3. 发布 GitHub Action

要将此项目作为 GitHub Action 发布，您需要：

1. **确保 action.yml 文件正确配置**：

   - 该文件已经配置好了，定义了 Action 的输入参数和运行环境

2. **创建发布版本**：

   - 在 GitHub 仓库页面，点击 "Releases"
   - 点击 "Create a new release"
   - 输入版本号（如 v1.0.0）
   - 添加发布说明
   - 点击 "Publish release"

3. **添加标签**：
   - 项目已包含自动标记最新版本的工作流（.github/workflows/versioning.yml）
   - 当您发布新版本时，它会自动添加 "latest" 标签

### 4. 在其他仓库中使用您的 Action

其他用户可以通过在他们的仓库中创建 `.github/workflows/ai-pr-reviewer.yml` 文件来
使用您的 Action：

```yaml
name: 代码审查

permissions:
  contents: read
  pull-requests: write

on:
  pull_request:
  pull_request_review_comment:
    types: [created]

concurrency:
  group:
    ${{ github.repository }}-${{ github.event.number || github.head_ref ||
    github.sha }}-${{ github.workflow }}-${{ github.event_name ==
    'pull_request_review_comment' && 'pr_comment' || 'pr' }}
  cancel-in-progress: ${{ github.event_name != 'pull_request_review_comment' }}

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: 您的用户名/ai-pr-reviewer@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        with:
          debug: false
          review_simple_changes: false
          review_comment_lgtm: false
```

### 5. 配置必要的 Secrets

用户需要在他们的仓库中配置以下 secrets：

- `GITHUB_TOKEN`：GitHub 自动提供
- `OPENAI_API_KEY`：用户需要从 OpenAI 获取 API 密钥并添加到仓库的 secrets 中
- `OPENAI_API_ORG`：（可选）如果用户有多个 OpenAI 组织

## 持续集成/持续部署

项目已经包含了一些 GitHub Actions 工作流：

- `versioning.yml`：自动为发布版本添加 "latest" 标签
- `openai-review.yml`：使用 Action 自身来审查项目的 PR
- `combine-prs.yml`：合并相关的 PR

## 自定义配置

用户可以通过修改 Action 的输入参数来自定义其行为：

- `debug`：启用调试模式
- `review_simple_changes`：是否审查简单变更
- `review_comment_lgtm`：即使代码看起来不错也留下评论
- `openai_light_model`：用于简单任务的模型
- `openai_heavy_model`：用于复杂任务的模型
- `system_message`：发送给 OpenAI 的系统消息
- 等等

详细配置选项可以在 [action.yml](./action.yml) 文件中找到。

## 故障排除

- 如果遇到权限问题，请确保工作流文件中的 `permissions` 部分配置正确
- 对于来自分叉的 PR，需要使用 `pull_request_target` 事件
- 如果需要查看与 OpenAI 服务器之间的消息，可以设置 `debug: true`

## 注意事项

- 代码将被发送到 OpenAI 的服务器进行处理，请在使用前确认合规性
- OpenAI API 有使用费用，特别是 `gpt-4` 模型的成本较高
- 此 Action 与 OpenAI 无关联
